{
  "name": "http-proxy-demo",
  "description": "调研如何使用 express-http-proxy ",
  "scripts": {
    "test": "npm -s run mocha",
    "mocha": "mocha -R spec spec/*.js"
  },
  "devDependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.3.1",
    "express-http-proxy": "^0.11.0",
    "http-proxy-middleware": "^0.17.4",
    "mocha": "^2.1.0",
    "supertest": "^3.0.0"
  }
}
